using System;

namespace calab // namespaces are groups of classess which used for organization fo the files and differentation
{
    class Program // class is a template which contain various members as methdos and variables
    {
        public static void Main(string[] args) // This is where excution starts // Static Methods belong to the class itself and don't depend on the instances so they can be called directly.
        {
            Console.WriteLine("Hello"); // argument in the method is called parameters.
           var value = Console.ReadLine(); // var keyword is useful when we don't know the type of data we are working on and it will assign the datatyp ebased on the input value
           
        }

    }
}


